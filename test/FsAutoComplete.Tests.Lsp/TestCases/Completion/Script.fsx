async {
    return 1
}
|> Async. // completion at this `.` should not have a billion suggestions


List.

let tail = List.

type A = {
  Id : Lis
}
open System.IO
Path.GetDirectoryName("foo").
Path.GetDirectoryName("foo")

"bareString".
"bareString"

[1;2;3].
[1;2;3]

$"{List.}"
$"{ List. }"

  #e